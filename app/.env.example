# Frontend (Vite, exposed to the browser — anon key only)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Serverless functions only (api/*) — never expose these to the client
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
ANTHROPIC_API_KEY=
